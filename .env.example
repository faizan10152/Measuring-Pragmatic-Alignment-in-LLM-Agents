# Required only by notebook 3 (agentic pipeline), and only when regenerating
# replies. The notebook reuses notebooks/pipeline_results.csv if it is present,
# so the analysis runs without a key.
# Free key: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here
